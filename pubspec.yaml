name: d_session
description: Easy way to access Shared Preference and use as Session.
version: 0.0.3
homepage: https://github.com/indratrisnar/d_session

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
